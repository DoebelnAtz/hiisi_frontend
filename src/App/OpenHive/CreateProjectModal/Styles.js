import styled from 'styled-components';
import { components, modal } from '../../../Styles/sharedStyles';

export const OutsideDiv = styled.div`
	${modal.outside};
`;

export const InsideDiv = styled.div`
	${modal.inside};
`;

export const TitleInput = styled.input`
	${components.input};
`;

