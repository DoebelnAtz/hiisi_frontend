import styled from 'styled-components';
import { length, color} from '../../../Styles/SharedStyles';

export const MobileMessagesPageContainer = styled.div`
	width: 100%;
	overflow-y: auto;
	height: calc(100%);
	background-color: ${color.siteBG1};
`;
