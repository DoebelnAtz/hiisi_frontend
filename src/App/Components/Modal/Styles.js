import styled from 'styled-components';

import { length, modal } from '../../../Styles/sharedStyles';

export const OutsideDiv = styled.div`
	${modal.outside}
`;

export const InsideDiv = styled.div`
	${modal.inside}
`;
