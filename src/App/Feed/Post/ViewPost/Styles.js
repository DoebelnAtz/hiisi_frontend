import styled from 'styled-components';

import {border, color, colorAdjust, length} from '../../../../Styles/sharedStyles'

export const CommentSection = styled.div`
padding-left: ${length.margin};
    margin: ${length.margin} 0;
    color: ${color.primary};
`;
