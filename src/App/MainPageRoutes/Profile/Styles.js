import styled from 'styled-components'

import { layout as layou, length } from '../../../Styles/SharedStyles';

export const ProfileButtonRow = styled.div`
	margin: 0 0 ${length.margin} ${length.margin};
	${layou.row};
`;
